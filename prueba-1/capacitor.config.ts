import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'prueba.v1',
  appName: 'prueba-1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
